# Hurda — Vue frontend

Vite + Vue 3 SPA for the **Scrap (Hurda) Management** application.

Setup, API reference, roles, and data model: see the root [`README.md`](../README.md).

```bash
npm install
npm run dev
```
